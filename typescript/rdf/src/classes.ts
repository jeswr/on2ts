import { ns } from "./enum";
import { interfaces } from "./interfaces";

/**
 * @description This is the RDF Schema for the RDF vocabulary terms in the RDF Namespace, defined in RDF 1.1 Concepts.
 * @title The RDF Concepts Vocabulary (RDF)
 * @type <http://www.w3.org/2002/07/owl#Ontology>
 */
export namespace classes {
    /** */
    export class rdf implements interfaces.rdf {
        value;
    }

    /** */
    export class Alt implements interfaces.Alt {
        value;
    }

    /** */
    export class Bag implements interfaces.Bag {
        value;
    }

    /** */
    export class HTML implements interfaces.HTML {
        value;
    }

    /** */
    export class List implements interfaces.List {
        value;
    }

    /** */
    export class PlainLiteral implements interfaces.PlainLiteral {
        value;
    }

    /** */
    export class Property implements interfaces.Property {
        value;
    }

    /** */
    export class Seq implements interfaces.Seq {
        value;
    }

    /** */
    export class Statement implements interfaces.Statement {
        value;
    }

    /** */
    export class XMLLiteral implements interfaces.XMLLiteral {
        value;
    }

    /** */
    export class first implements interfaces.first {
        value;
    }

    /** */
    export class langString implements interfaces.langString {
        value;
    }

    /** */
    export class nil implements interfaces.nil {
        value;
    }

    /** */
    export class Object implements interfaces.Object {
        value;
    }

    /** */
    export class predicate implements interfaces.predicate {
        value;
    }

    /** */
    export class rest implements interfaces.rest {
        value;
    }

    /** */
    export class subject implements interfaces.subject {
        value;
    }

    /** */
    export class type implements interfaces.type {
        value;
    }

    /** */
    export class value implements interfaces.value {
        value;
    }
}
